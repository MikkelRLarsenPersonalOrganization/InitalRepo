name: Test ARC Runner

# Gør workflow kørbar manuelt
on:
  workflow_dispatch:

jobs:
  test-runner:
    runs-on: arc-runner-set
    steps:
      - name: Check runner
        run: echo "Runner is working!"
